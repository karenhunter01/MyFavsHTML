---database oam
-- user karen

Create table books (
	id int unsigned not null auto_increment,
	primary key (id),
	fname varchar( 65),
 	lname  varchar (100),
	title  varchar (500),
	series varchar (200)
);

